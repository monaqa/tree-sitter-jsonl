{
  "name": "tree-sitter-jsonl",
  "version": "0.1.0",
  "description": "JSON Lines grammar for tree-sitter",
  "main": "bindings/node",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "monaqa",
  "license": "MIT",
  "dependencies": {
    "nan": "^2.15.0"
  },
  "devDependencies": {
    "tree-sitter-cli": "^0.20.1",
    "tree-sitter-json": "^0.19.0"
  }
}
